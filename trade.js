const coreAssets = [
  { symbol: 'BTC', name: 'Bitcoin', pair: 'BTC / USD', id: 'bitcoin', price: 67842.90, change: 2.84, cap: '$1.34T', icon: 'bitcoin', decimals: 2 },
  { symbol: 'ETH', name: 'Ethereum', pair: 'ETH / USD', id: 'ethereum', price: 3842.16, change: 4.12, cap: '$462.8B', icon: 'ethereum', decimals: 2 },
  { symbol: 'SOL', name: 'Solana', pair: 'SOL / USD', id: 'solana', price: 168.42, change: -1.28, cap: '$78.1B', icon: 'solana', decimals: 2 },
  { symbol: 'XRP', name: 'XRP', pair: 'XRP / USD', id: 'ripple', price: 0.5924, change: 0.66, cap: '$33.4B', icon: 'xrp', decimals: 4 },
  { symbol: 'AVAX', name: 'Avalanche', pair: 'AVAX / USD', id: 'avalanche-2', price: 34.18, change: -0.74, cap: '$13.4B', icon: 'ethereum', decimals: 2 }
];
const extraCatalog = [
  ['DOGE','Dogecoin','dogecoin',5],['ADA','Cardano','cardano',4],['DOT','Polkadot','polkadot',2],['LINK','Chainlink','chainlink',2],['MATIC','Polygon','matic-network',4],['SHIB','Shiba Inu','shiba-inu',8],['LTC','Litecoin','litecoin',2],['BCH','Bitcoin Cash','bitcoin-cash',2],['UNI','Uniswap','uniswap',2],['ATOM','Cosmos','cosmos',2],['XLM','Stellar','stellar',5],['NEAR','NEAR Protocol','near',2],['APT','Aptos','aptos',2],['FIL','Filecoin','filecoin',2],['ARB','Arbitrum','arbitrum',4],['OP','Optimism','optimism',4],['INJ','Injective','injective-protocol',2],['HBAR','Hedera','hedera-hashgraph',5],['VET','VeChain','vechain',5],['MKR','Maker','maker',2],['ICP','Internet Computer','internet-computer',2],['AAVE','Aave','aave',2],['GRT','The Graph','the-graph',5],['ALGO','Algorand','algorand',5],['SAND','The Sandbox','the-sandbox',4],['MANA','Decentraland','decentraland',4],['EOS','EOS','eos',4],['XTZ','Tezos','tezos',4],['THETA','Theta Network','theta',4],['FLOW','Flow','flow',4],['AXS','Axie Infinity','axie-infinity',2],['CHZ','Chiliz','chiliz',5],['EGLD','MultiversX','elrond-egld',2],['SNX','Synthetix','havven',2],['KAVA','Kava','kava',4],['RUNE','THORChain','thorchain',3],['QNT','Quant','quant-network',2],['CRV','Curve DAO','curve-dao-token',4],['LDO','Lido DAO','lido-dao',4],['COMP','Compound','compound-governance-token',2],['MKR','Maker','maker',2],['RPL','Rocket Pool','rocket-pool',4],['ENS','Ethereum Name Service','ethereum-name-service',2],['PEPE','Pepe',' pepe',8],['WIF','dogwifhat','dogwifcoin',6],['BONK','Bonk','bonk',8],['FLOKI','FLOKI','floki',8],['ORDI','ORDI','ordi',4],['SUI','Sui','sui',4],['SEI','Sei','sei-network',4],['TIA','Celestia','celestia',4],['JUP','Jupiter','jupiter-exchange-solana',4],['ONDO','Ondo','ondo-finance',4],['PYTH','Pyth Network','pyth-network',4],['WLD','Worldcoin','worldcoin-wld',4],['FET','Artificial Superintelligence Alliance','fetch-ai',4],['RENDER','Render','render-token',3],['TAO','Bittensor','bittensor',2],['KAS','Kaspa','kaspa',5],['ETC','Ethereum Classic','ethereum-classic',2],['XMR','Monero','monero',2],['ZEC','Zcash','zcash',2],['DASH','Dash','dash',2],['NEO','NEO','neo',2],['IOTA','IOTA','iota',5],['WAVES','Waves','waves',4],['1INCH','1inch','1inch',4],['BAT','Basic Attention Token','basic-attention-token',4],['ZRX','0x','0x',4],['YFI','yearn.finance','yearn-finance',2],['BAL','Balancer','balancer',3],['SUSHI','Sushi','sushi',4],['CAKE','PancakeSwap','pancakeswap-token',4],['GMX','GMX','gmx',3],['DYDX','dYdX','dydx',3],['LUNC','Terra Luna Classic','terra-luna',6],['USTC','TerraClassicUSD','terrausd',6],['KCS','KuCoin Token','kucoin-shares',3],['HT','Huobi','huobi-token',4],['OKB','OKB','okb',2],['LEO','LEO Token','leo-token',4],['TUSD','TrueUSD','true-usd',4],['USDP','Pax Dollar','pax-dollar',4],['USDD','USDD','usdd',4],['DAI','Dai','dai',4],['USDC','USD Coin','usd-coin',4],['USDT','Tether','tether',4],['FDUSD','First Digital USD','first-digital-usd',4],['FRAX','Frax','frax',4],['PAXG','PAX Gold','pax-gold',2],['XAUT','Tether Gold','tether-gold',2],['IMX','Immutable','immutable-x',4],['GALA','GALA','gala',5],['ENJ','Enjin Coin','enjincoin',4],['ILV','Illuvium','illuvium',2],['APE','ApeCoin','apecoin',4],['BLUR','Blur','blur',4],['RAY','Raydium','raydium',4],['JTO','Jito','jito-staked-sol',4],['MANTA','Manta Network','manta-network',4],['STRK','Starknet','starknet',4],['ZK','ZKsync','zksync',4],['MINA','Mina Protocol','mina-protocol',4],['ROSE','Oasis Network','oasis-network',5],['ONE','Harmony','harmony',5],['CELO','Celo','celo',4],['KSM','Kusama','kusama',3],['AR','Arweave','arweave',3],['STX','Stacks','blockstack',4],['FLOW','Flow','flow',4],['LPT','Livepeer','livepeer',3],['ANKR','Ankr','ankr',5],['OCEAN','Ocean Protocol','ocean-protocol',4],['SKL','SKALE','skale',5],['COTI','COTI','coti',5],['RVN','Ravencoin','ravencoin',5],['DCR','Decred','decred',3],['QTUM','Qtum','qtum',3],['ICX','ICON','icon',4],['SC','Siacoin','siacoin',6],['ONT','Ontology','ontology',4],['WOO','WOO Network','woo-network',4],['SFP','SafePal','safepal',4],['TWT','Trust Wallet Token','trust-wallet-token',4]
];
const assets = [...coreAssets, ...extraCatalog.map(([symbol, name, id, decimals]) => ({ symbol, name, pair: `${symbol} / USD`, id: id.trim(), price: 0, change: 0, cap: '—', icon: 'ethereum', decimals }))].filter((asset, index, all) => all.findIndex(item => item.symbol === asset.symbol) === index);
const watchlist = ['BTC', 'ETH', 'SOL'];
let marketExpanded = false;
const $ = (selector) => document.querySelector(selector);
const formatPrice = (value, decimals = 2) => value.toLocaleString('en-US', { minimumFractionDigits: decimals, maximumFractionDigits: decimals });

function coinMarkup(asset, small = false) { return `<span class="coin-icon ${asset.icon}${small ? ' small' : ''}">${asset.symbol[0]}</span>`; }
function changeMarkup(change) { return `<span class="${change >= 0 ? 'positive' : 'negative'}">${change >= 0 ? '+' : ''}${change.toFixed(2)}%</span>`; }

function renderTicker() {
  $('#tickerItems').innerHTML = assets.slice(0, 4).map(asset => `<div class="ticker-item" data-pair="${asset.symbol}"><span class="mini-name">${asset.symbol}/USD</span><strong>${formatPrice(asset.price, asset.decimals)}</strong>${changeMarkup(asset.change)}</div>`).join('');
  document.querySelectorAll('.ticker-item').forEach(item => item.addEventListener('click', () => selectAsset(item.dataset.pair)));
}
function renderMarkets(filter = '') {
  const filtered = assets.filter(asset => `${asset.name} ${asset.symbol}`.toLowerCase().includes(filter.toLowerCase()));
  const visible = filter || marketExpanded ? filtered : filtered.slice(0, 8);
  $('#marketRows').innerHTML = visible.map(asset => `<div class="table-row market-row" data-pair="${asset.symbol}"><span class="asset-cell">${coinMarkup(asset, true)}<span><strong>${asset.name}</strong><small>${asset.symbol} / USD</small></span></span><span class="row-price">$${formatPrice(asset.price, asset.decimals)}</span><span class="row-change">${changeMarkup(asset.change)}</span><span class="row-cap">${asset.cap}</span><button class="star ${watchlist.includes(asset.symbol) ? 'active' : ''}" data-star="${asset.symbol}" aria-label="${watchlist.includes(asset.symbol) ? 'Remove' : 'Add'} ${asset.symbol} ${watchlist.includes(asset.symbol) ? 'from' : 'to'} favourites">${watchlist.includes(asset.symbol) ? '★' : '☆'}</button></div>`).join('') || '<div class="empty-state">No assets found</div>';
  document.querySelectorAll('.market-row').forEach(row => row.addEventListener('click', event => { if (!event.target.closest('[data-star]')) selectAsset(row.dataset.pair); }));
  document.querySelectorAll('[data-star]').forEach(button => button.addEventListener('click', () => toggleWatch(button.dataset.star)));
  const showMore = $('#showMoreMarkets');
  if (showMore) showMore.remove();
  if (!filter && !marketExpanded && filtered.length > visible.length) { const button = document.createElement('button'); button.id = 'showMoreMarkets'; button.className = 'view-all show-more'; button.innerHTML = `Show more pairs <span>↓</span><small>${filtered.length - visible.length} more available</small>`; button.addEventListener('click', () => { marketExpanded = true; renderMarkets(); }); $('#marketRows').after(button); }
}
function renderWatchlist() {
  $('#watchItems').innerHTML = watchlist.map(symbol => { const asset = assets.find(item => item.symbol === symbol); return `<div class="watch-item" data-pair="${asset.symbol}">${coinMarkup(asset, true)}<div class="watch-info"><strong>${asset.symbol} / USD</strong><small>${asset.name}</small></div><span class="watch-price">$${formatPrice(asset.price, asset.decimals)}<br>${changeMarkup(asset.change)}</span></div>`; }).join('');
  $('#emptyFavourites').hidden = watchlist.length > 0;
  document.querySelectorAll('.watch-item').forEach(item => item.addEventListener('click', () => selectAsset(item.dataset.pair)));
}
function toggleWatch(symbol) { const position = watchlist.indexOf(symbol); position >= 0 ? watchlist.splice(position, 1) : watchlist.push(symbol); renderMarkets($('#marketSearch').value); renderWatchlist(); }

let chartType = 'candles';
let selectedTimeframe = '1D';
let currentSession = null;
function drawChart(asset = assets[0]) {
  const svg = $('#priceChart');
  const seed = asset.price;
  const timeframeSettings = { '1H': [30, .003, .00012], '1D': [42, .006, .00033], '1W': [56, .011, .00052], '1M': [64, .017, .0008], '1Y': [72, .026, .0011] };
  const [pointCount, volatility, trendRate] = timeframeSettings[selectedTimeframe] || timeframeSettings['1D'];
  const points = Array.from({ length: pointCount }, (_, i) => { const wave = Math.sin(i * .52) * seed * volatility; const trend = i * seed * trendRate; const noise = Math.cos(i * 1.74) * seed * volatility * .42; return seed - seed * volatility * 4.5 + trend + wave + noise; });
  const min = Math.min(...points) * .994, max = Math.max(...points) * 1.006;
  const coords = points.map((value, index) => `${(index / (points.length - 1)) * 900},${320 - ((value - min) / (max - min)) * 300}`);
  const area = `0,320 ${coords.join(' ')} 900,320`;
  const grid = [20, 95, 170, 245, 320].map(y => `<line class="chart-grid-line" x1="0" y1="${y}" x2="900" y2="${y}"/>`).join('');
  const lineChart = `<polygon class="area-fill" points="${area}"/><polyline class="price-glow" points="${coords.join(' ')}"/><polyline class="price-line-path" points="${coords.join(' ')}"/><circle class="chart-point" cx="900" cy="${320 - ((points[points.length - 1] - min) / (max - min)) * 300}" r="5"/>`;
  const chartStep = 900 / Math.max(points.length - 1, 1);
  const candleChart = points.slice(0, -1).map((close, index) => { const open = index === 0 ? close - seed * .002 : points[index - 1]; const high = Math.max(open, close) + seed * (.003 + (index % 4) * .0005); const low = Math.min(open, close) - seed * (.002 + (index % 3) * .0004); const x = 10 + index * chartStep; const y = value => 320 - ((value - min) / (max - min)) * 300; const bodyY = Math.min(y(open), y(close)); const bodyH = Math.max(3, Math.abs(y(open) - y(close))); return `<line class="candle-wick" x1="${x}" y1="${y(high)}" x2="${x}" y2="${y(low)}"/><rect class="candle-body ${close >= open ? 'candle-up' : 'candle-down'}" x="${x - Math.max(2, chartStep * .22)}" y="${bodyY}" width="${Math.max(4, chartStep * .44)}" height="${bodyH}"/>`; }).join('');
  const barChart = points.slice(0, -1).map((close, index) => { const open = index === 0 ? close : points[index - 1]; const high = Math.max(open, close) + seed * .003; const low = Math.min(open, close) - seed * .003; const x = 10 + index * chartStep; const y = value => 320 - ((value - min) / (max - min)) * 300; return `<line class="candle-wick" x1="${x}" y1="${y(high)}" x2="${x}" y2="${y(low)}"/><line class="candle-wick" x1="${x - 5}" y1="${y(open)}" x2="${x}" y2="${y(open)}"/><line class="candle-wick" x1="${x}" y1="${y(close)}" x2="${x + 5}" y2="${y(close)}"/>`; }).join('');
  const chartContent = chartType === 'line' ? lineChart : chartType === 'bars' ? barChart : candleChart;
  svg.innerHTML = `<defs><linearGradient id="areaGradient" x1="0" x2="0" y1="0" y2="1"><stop offset="0" stop-color="#e3f24f" stop-opacity=".28"/><stop offset="1" stop-color="#e3f24f" stop-opacity="0"/></linearGradient></defs>${grid}${chartContent}`;
}
function selectAsset(symbol) { const asset = assets.find(item => item.symbol === symbol) || assets[0]; $('#selectedPair').textContent = asset.pair; $('#selectedPrice').textContent = formatPrice(asset.price, asset.decimals); $('#selectedChange').outerHTML = changeMarkup(asset.change).replace('<span', '<span id="selectedChange"'); $('#selectedBadge').textContent = asset.symbol; $('#selectedCoinIcon').className = `coin-icon ${asset.icon}`; $('#selectedCoinIcon').textContent = asset.symbol[0]; $('#chartPairMeta').textContent = `${asset.symbol}/USD · SPOT`; $('#orderAsset').textContent = asset.name; $('#orderPair').textContent = asset.pair; $('#orderCoinIcon').className = `coin-icon ${asset.icon} small`; $('#orderCoinIcon').textContent = asset.symbol[0]; $('.order-summary div:first-child span').textContent = `Estimated ${asset.symbol}`; drawChart(asset); updateEstimate(); }
function updateEstimate() { const amount = Number($('#amountInput').value) || 0; const asset = assets.find(item => item.name === $('#orderAsset').textContent) || assets[0]; $('#estimatedCoin').textContent = `${(amount / asset.price).toFixed(asset.symbol === 'BTC' ? 5 : 4)} ${asset.symbol}`; $('#orderTotal').textContent = `$${amount.toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`; }

async function fetchLivePrices() { try { const ids = assets.map(asset => asset.id).join(','); const response = await fetch(`https://api.coingecko.com/api/v3/simple/price?ids=${ids}&vs_currencies=usd&include_24hr_change=true&include_market_cap=true`); if (!response.ok) throw new Error('Market API unavailable'); const prices = await response.json(); assets.forEach(asset => { const live = prices[asset.id]; if (!live) return; asset.price = live.usd; asset.change = live.usd_24h_change || asset.change; asset.cap = live.usd_market_cap ? `$${(live.usd_market_cap / 1e9).toFixed(1)}B` : asset.cap; }); $('#priceSource').textContent = 'CoinGecko live'; renderTicker(); renderMarkets($('#marketSearch').value); renderWatchlist(); selectAsset(assets.find(item => item.pair === $('#selectedPair').textContent)?.symbol || 'BTC'); } catch (error) { $('#priceSource').textContent = 'Demo fallback'; } }

function openAuth(mode = 'login') { $('#authOverlay').classList.add('open'); $('#authOverlay').setAttribute('aria-hidden', 'false'); setAuthMode(mode); }
function closeAuth() { $('#authOverlay').classList.remove('open'); $('#authOverlay').setAttribute('aria-hidden', 'true'); }
function setAuthMode(mode) { document.querySelectorAll('.auth-tab').forEach(tab => tab.classList.toggle('active', tab.dataset.authTab === mode)); $('#authSubmitText').textContent = mode === 'login' ? 'Log in to Dream Trade' : 'Create my account'; $('.auth-copy .eyebrow').textContent = mode === 'login' ? 'WELCOME BACK' : 'START YOUR JOURNEY'; $('.auth-copy h2').textContent = mode === 'login' ? 'Make your next move.' : 'Your market, your way.'; }
function updateTimeGreeting() { const now = new Date(); const hour = now.getHours(); const greeting = hour < 12 ? 'Good morning' : hour < 18 ? 'Good afternoon' : 'Good evening'; const date = now.toLocaleDateString('en-US', { weekday: 'long', day: '2-digit', month: 'short', year: 'numeric' }).toUpperCase().replace(',', ' /'); $('#greetingHeading').innerHTML = `${greeting}, <em>trader.</em>`; $('#dateLabel').textContent = date; }
function updateProfile(session) { currentSession = session; const user = session?.user; const name = user?.user_metadata?.full_name || user?.user_metadata?.name || user?.email?.split('@')[0] || 'Guest trader'; $('.profile-copy strong').textContent = user ? name : 'Guest trader'; $('.profile-copy small').textContent = user ? 'Connected account · Log out' : 'Connect account'; $('.avatar').textContent = user ? name.slice(0, 2).toUpperCase() : 'JD'; }

const authClient = window.supabase?.createClient && !window.DREAM_TRADE_SUPABASE_URL.includes('YOUR_') && !window.DREAM_TRADE_SUPABASE_ANON_KEY.includes('YOUR_') ? window.supabase.createClient(window.DREAM_TRADE_SUPABASE_URL, window.DREAM_TRADE_SUPABASE_ANON_KEY) : null;
function setupSupabaseAuth() { const googleButton = $('#googleSignIn'); if (!googleButton) return; googleButton.addEventListener('click', async () => { if (!authClient) return showToast('Auth needs setup', 'Paste your Supabase URL and anon key into supabase-config.js.'); const { error } = await authClient.auth.signInWithOAuth({ provider: 'google', options: { redirectTo: window.location.href } }); if (error) showToast('Google sign-in failed', error.message); }); if (authClient) { authClient.auth.getSession().then(({ data }) => updateProfile(data.session)); authClient.auth.onAuthStateChange((event, session) => { updateProfile(session); if (session?.user && event === 'SIGNED_IN') { closeAuth(); const name = session.user.user_metadata?.full_name || session.user.email?.split('@')[0] || 'trader'; showToast('Signed in successfully', `Welcome ${name} to Dream Trade.`); } }); } }

updateTimeGreeting(); setInterval(updateTimeGreeting, 60000); renderTicker(); renderMarkets(); renderWatchlist(); drawChart(); updateEstimate();
fetchLivePrices();
$('#marketSearch').addEventListener('input', e => { marketExpanded = false; renderMarkets(e.target.value); });
$('#amountInput').addEventListener('input', updateEstimate);
$('#addFavourite').addEventListener('click', () => { $('#marketSearch').focus(); showToast('Add a favourite', 'Search for a pair, then click its star.'); });
$('#manageFavourites').addEventListener('click', () => { document.querySelector('.watch-panel').scrollIntoView({ behavior: 'smooth', block: 'center' }); });
document.querySelectorAll('[data-open-auth]').forEach(button => button.addEventListener('click', () => openAuth()));
$('#profileButton').addEventListener('click', async () => { if (currentSession && authClient) { const { error } = await authClient.auth.signOut(); if (error) showToast('Logout failed', error.message); else { updateProfile(null); showToast('Logged out', 'Your Dream Trade session has ended.'); } } else openAuth(); });
$('[data-close-auth]').addEventListener('click', closeAuth);
$('#authOverlay').addEventListener('click', e => { if (e.target.id === 'authOverlay') closeAuth(); });
document.querySelectorAll('[data-auth-tab]').forEach(tab => tab.addEventListener('click', () => setAuthMode(tab.dataset.authTab)));
$('.show-password').addEventListener('click', e => { const input = e.target.previousElementSibling; input.type = input.type === 'password' ? 'text' : 'password'; e.target.textContent = input.type === 'password' ? 'Show' : 'Hide'; });
$('#authForm').addEventListener('submit', async e => { e.preventDefault(); const submit = $('.auth-submit'); submit.disabled = true; const form = new FormData(e.target); const email = form.get('email'); const password = form.get('password'); try { if (!authClient) throw new Error('Paste your Supabase URL and anon key into supabase-config.js.'); const mode = document.querySelector('.auth-tab.active').dataset.authTab; const result = mode === 'signup' ? await authClient.auth.signUp({ email, password }) : await authClient.auth.signInWithPassword({ email, password }); if (result.error) throw result.error; if (mode === 'signup') showToast('Check your email', 'Confirm your Dream Trade account before signing in.'); else showToast('Signed in successfully', `Welcome back to Dream Trade.`); } catch (error) { showToast('Email sign-in failed', error.message); } finally { submit.disabled = false; } });
$('#orderButton').addEventListener('click', () => showToast('Demo order ready', 'Your order review is waiting for account connection.'));
document.querySelectorAll('.order-tab').forEach(tab => tab.addEventListener('click', () => { document.querySelectorAll('.order-tab').forEach(item => item.classList.remove('active')); tab.classList.add('active'); $('#orderButton').innerHTML = `Review ${tab.classList.contains('sell') ? 'sell' : 'buy'} order <span>↗</span>`; }));
document.querySelectorAll('.timeframes button').forEach(button => button.addEventListener('click', () => { selectedTimeframe = button.textContent.trim(); document.querySelectorAll('.timeframes button').forEach(item => item.classList.remove('selected')); button.classList.add('selected'); drawChart(assets.find(item => item.pair === $('#selectedPair').textContent) || assets[0]); }));
document.querySelectorAll('[data-chart-type]').forEach(button => button.addEventListener('click', () => { chartType = button.dataset.chartType; document.querySelectorAll('[data-chart-type]').forEach(item => item.classList.toggle('active', item === button)); drawChart(assets.find(item => item.pair === $('#selectedPair').textContent) || assets[0]); }));
$('#themeToggle').addEventListener('click', () => { document.body.classList.toggle('dark-mode'); localStorage.setItem('dream-trade-theme', document.body.classList.contains('dark-mode') ? 'dark' : 'light'); });
if (localStorage.getItem('dream-trade-theme') === 'dark') document.body.classList.add('dark-mode');
setupSupabaseAuth();
document.addEventListener('pointermove', event => { const rocket = $('#rocket'); if (!rocket) return; rocket.style.setProperty('--rocket-x', `${(event.clientX / window.innerWidth - .5) * 22}px`); rocket.style.setProperty('--rocket-y', `${(event.clientY / window.innerHeight - .5) * 14}px`); });
document.addEventListener('click', event => { if (!event.target.closest('button,input,a,.auth-card')) { const rocket = $('#rocket'); rocket.classList.remove('launch'); void rocket.offsetWidth; rocket.classList.add('launch'); setTimeout(() => rocket.classList.remove('launch'), 1400); } });
let fundingNetwork = 'BTC';
let generatedAddress = '';
document.querySelectorAll('.network-tab').forEach(button => button.addEventListener('click', () => { fundingNetwork = button.dataset.network; document.querySelectorAll('.network-tab').forEach(item => item.classList.toggle('active', item === button)); $('#networkName').textContent = fundingNetwork; generatedAddress = ''; $('#simulateDeposit').disabled = true; $('#addressCard').innerHTML = '<div class="address-empty"><span class="qr-placeholder">⌗</span><strong>No address generated</strong><small>Generate a sandbox address to continue.</small></div>'; }));
$('#generateAddress').addEventListener('click', () => { const prefixes = { BTC: 'bc1q', ETH: '0x', USDC: '0x' }; const suffix = Math.random().toString(36).slice(2) + Math.random().toString(36).slice(2); generatedAddress = `${prefixes[fundingNetwork]}${suffix}${suffix.slice(0, 12)}`; $('#addressCard').innerHTML = `<div class="address-live"><p>${fundingNetwork} sandbox deposit address</p><div class="address-value"><code>${generatedAddress}</code><button class="copy-address" id="copyAddress" aria-label="Copy deposit address">⧉</button></div><small>✓ Address active for this demo session</small></div>`; $('#simulateDeposit').disabled = false; $('#copyAddress').addEventListener('click', async () => { await navigator.clipboard?.writeText(generatedAddress); showToast('Address copied', 'Use the sandbox simulator to reflect a test deposit.'); }); });
$('#simulateDeposit').addEventListener('click', () => { if (!generatedAddress) return; const balance = Number(localStorage.getItem('dream-trade-balance') || 0) + 250; localStorage.setItem('dream-trade-balance', balance); $('#demoBalance').textContent = `$${balance.toLocaleString('en-US', { minimumFractionDigits: 2 })}`; showToast('Sandbox deposit credited', 'This simulated balance is not real money.'); });
$('#demoBalance').textContent = `$${Number(localStorage.getItem('dream-trade-balance') || 0).toLocaleString('en-US', { minimumFractionDigits: 2 })}`;
function showToast(title, message) { $('.toast strong').textContent = title; $('.toast small').textContent = message; $('#toast').classList.add('show'); setTimeout(() => $('#toast').classList.remove('show'), 3500); }
setInterval(fetchLivePrices, 30000);
